# 安全边界与报告

本项目是未经完整真实环境验收的 Alpha，未通过外部安全审计。不要向公开 Issue、聊天或代码仓库提交真实验证码、令牌、Cookie、完整认证响应、凭据文件、含收听记录的数据库或未脱敏截图。

## 已采取的约束

- Wails 仅绑定一个业务 `Call` 方法，动作采用允许列表；没有通用 HTTP 代理、执行命令或任意路径读取接口。
- 平台请求的域名固定。拒绝 HTTP 重定向和解析到非公网地址的 API 目标；不会关闭 TLS 校验或伪造移动设备身份。
- 音频仅允许明确的 HTTPS 媒体域名；不把账号令牌附加到 URL，不转发给 CDN。
- HTML 说明解析到惰性模板后，重新构造白名单节点；不复制原始属性，不渲染脚本、图片、iframe、SVG 或表单。
- 会话刷新合并并发请求；每个原请求最多刷新重放一次；429 冷却、403 明确失败。验证码不自动重发。
- epoch 校验与写盘 / 退出清理处于同一锁内；旧登录、恢复、刷新、列表和进度响应不允许写入新会话。
- Windows 凭据使用用户级 DPAPI；不使用机器级保护。系统 SQLite、凭据和原生集成 DLL 按绝对 System32 路径加载。
- SQLite 使用绑定参数；全量列表仅在明确结束后原子替换缓存。未完成同步不删除上次完整数据。
- 演示服务器仅绑定 127.0.0.1，校验 Host、Origin / Fetch Metadata，并使用随机本次进程令牌保护 API。

## 仍需验证 / 已知边界

DPAPI、Win32 ABI、托盘、媒体键、WebView2 CSP 及退出顺序的 Windows 测试需要真实执行；当前交叉编译不能替代它。SQLite DLL 由 Windows 提供，系统补丁状态不由项目管理。WebView2 内存、文件缓存和具有同用户权限的恶意进程不在强隔离保证内。

播放资源仅允许当前配置的两个媒体域名；其他合法 CDN 也可能被拒绝。不能通过关闭 CSP、TLS 或任意放开 URL 来处理兼容问题，应先核实来源、补充回归测试再评审修改白名单。

私有 API 兼容性、平台授权、付费内容许可、代码签名、完整依赖 SBOM 与漏洞审计均未完成。不会因为开源或免费而默认取得平台许可。

## 报告方式

仓库所有者尚未指定安全联系地址，不能虚构一个。公开托管前应配置 GitHub 私密漏洞报告入口或实际可用的安全邮箱。配置完成前，保留问题的最小化合成复现，在授权的私下渠道联系仓库所有者。不要公开真实凭据或未修复漏洞的敏感细节。
