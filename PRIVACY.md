# 隐私与本地数据

Starling 没有项目方云端、遥测 SDK、广告、自动上传诊断或账号代理服务器。

正常使用仍会发生网络请求：公开页面访问小宇宙网站；账号接入访问小宇宙 API 和主播后台认证 API；音频请求指向受允许的媒体 CDN；有效封面可从受允许的图片 CDN 加载。平台、CDN、操作系统或默认浏览器自身的数据处理不由本项目控制。

手机号和验证码只用于用户主动发起的认证流程，不写入 SQLite、诊断日志或前端持久化存储。访问令牌与刷新令牌只在 Go 层使用；选择保存会话时，通过当前 Windows 用户的 DPAPI 加密后保存到 `session.vault`。未选择保存时，凭据保留在进程内存中。系统保护不可用时不会降级为明文保存。

本机 `metadata.db` 保存标题等元数据、完整个人库缓存、近期更新快照、搜索历史、书签、队列、进度及设置；不加密全部收听历史，因此具有相同 Windows 用户权限的程序可读取它。数据库不能被描述为整个设备上的隐私保险箱。

用户主动搜索时，关键词会发送到平台；搜索历史按账号保存在本机，可在探索页清除。用户主动订阅时，节目标识与账号认证请求发送到平台；本机退出或清除缓存不撤销平台订阅关系。未知结果需要读回核对，没有自动重发写入。

评论列表和草稿仅在本次进程内按账号、单集保存，不写入 SQLite 或前端持久化存储，退出账号清除。用户主动点击发表评论时，正文和单集标识会通过账号认证请求发送到小宇宙平台，形成公开评论；本机清除草稿、退出账号或取消等待不会删除平台已经接收的评论。后台只保留当前账号会话内的发表请求 ID 用于防重复，不在诊断中记录正文、账号标识或原始响应。没有离线待发送队列或自动重发。

API 请求携带本次客户端运行随机生成的 UUID（`x-jike-device-id`）。它不来自硬件、手机号或其他应用，不写入数据库或诊断；同次运行保持稳定，下次运行重新生成。User-Agent 仍标识为 Starling，不伪造手机型号或系统版本。

退出账号会清除本应用的受保护凭据和当前账号命名空间的元数据、缓存、队列、书签、进度；访客数据保持隔离。清除缓存不影响进度；重置数据会同时清除访客数据。运行中的 WebView 可能持有音频或图片缓存；采用独立临时运行目录，在退出 / 下次启动时尽力删除，不承诺即时或取证级安全擦除。

诊断仅包含版本、平台、当前会话状态，以及本次进程最近 100 条业务方法名、错误码与时间；请求被拒绝时可包含 HTTP 状态和数字业务码，不包含原始错误响应。只有用户选择文件对话框后才写出，不自动上传。请在分享任何诊断或截图前复核内容。

`cmd/demo` 是独立演示服务器，仅使用合成内容和临时数据库；页面明确标记，不应输入真实凭据，也不会连接平台账号服务。
