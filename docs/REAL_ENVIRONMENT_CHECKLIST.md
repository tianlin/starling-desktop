# 真实环境验收清单

只使用已获准测试的账号。验证码与令牌留在应用和本机，不要发到聊天、Issue 或测试报告中。本文件记录完整验收要求；单项构建和启动结果见 TEST_REPORT.md，不将局部结果当作整项验收。

## G1 接入与发布

- [ ] 明确平台允许的登录、读取个人库、音频播放及公开分发边界。
- [ ] 需要时取得平台许可和专业法律评估；明确商标 / 内容许可。
- [ ] Wails 子模块完成依赖下载、go.sum 锁定、SBOM、许可证与漏洞检查。
- [ ] 决定是否保留或关闭实验适配器；不同意接入边界时不公开分发账号功能。

## G2 账号

- [ ] 短信验证码能正常发送和验证；错误 / 过期验证码不会无限重试。
- [ ] 主播后台返回身份与听众资料一致；账号类型不兼容时明确拒绝。
- [ ] 在集合稳定期间，核对两页以上订阅、两页以上收藏，与手机端没有缺漏或重复。
- [x] 确认本次真实账号收藏响应的分页字段与末页省略语义；2026-09-12 两页已验证，其他账号/更大集合与手机对照另行验收。
- [ ] 访问令牌过期后实际刷新成功，凭据轮换原子保存；并发只刷新一次。
- [ ] 403、429、网络故障、长期失效能准确区分并避免反复请求。
- [ ] 登录失败后 UI 的 epoch / 状态同步；退出中迟到请求不能恢复会话。
- [ ] 两个账号与访客状态相互隔离；正常重启恢复身份需要平台核对。

## G3 Windows 11 x64

- [x] 在可联网机器构建 Wails production 候选；实际 go.sum 已更新，Go 1.26.8 与哈希见 TEST_REPORT；本次采用跳过绑定生成的 Candidate 流程。
- [x] 运行所有根模块测试，特别是原生 DPAPI 和 Win32 ABI 测试；87 个测试/子测试通过，显式联网项默认跳过并另记实测。
- [ ] 当前用户 DPAPI 可保存 / 读取；复制到另一 Windows 用户后不可解密。
- [ ] Windows SQLite 实际绑定、中文路径、文件占用、磁盘满与损坏恢复路径验证。
- [ ] 在 WebView2 中加载本地 UI；CSP 不破坏 Wails 桥，又能阻止恶意内容和越域媒体。
- [ ] 免费可访问音频的 MP3 / AAC / M4A、Range / 非 Range、超长节目、定位 / 倍速验证。
- [ ] 连续 2 小时以上播放、切换页面、隐藏窗口、锁屏；统计中断与资源占用。
- [ ] 托盘显示 / 播放暂停 / 退出，Explorer 重启后恢复图标；托盘失败时窗口不被隐藏。
- [ ] 实体媒体键正常；与浏览器 Media Session 不双触发，与其他播放器竞争时有明确行为。
- [ ] 睡眠、唤醒、蓝牙断开 / 音频设备切换不意外播放。
- [ ] 正常退出保存最终位置；强退后只恢复最后成功检查点；UI 不宣称零进度损失。
- [ ] 100% / 125% / 150% / 200% DPI、键盘焦点、屏幕阅读器与窗口最小尺寸验证。
- [ ] 无 WebView2 / 离线 / 无管理员权限的干净机器行为验证。
- [ ] 当前用户安装、已有程序占用、升级覆盖、保留 / 删除数据卸载验证。
- [ ] 完成代码签名和发布流程后才提供正式安装包；不要指导用户关闭系统防护。

记录模板：场景、账号类型（不写账号标识）、OS / WebView2 版本、步骤、预期、实际、脱敏证据、结论、提交 SHA、执行日期。任何失败都阻止对应门槛通过。
