# 遗留工作与验收状态

更新：2026-09-12。依据 PRD、PRD_TRACEABILITY、REAL_ENVIRONMENT_CHECKLIST 和 TEST_REPORT 逐类整理。未改写 PRD 的 G1～G3 通过条件。

评论增量：已实现热门/最新双排序、已登录阅读、展开回复、一级文字发表及楼中楼回复、防重复/不确定结果处理，测试和候选构建见 TEST_REPORT。真实 HOT/TIME 各两页及两个回复线程读取已核验；真实发表评论/回复、审核/评论关闭表现和复杂线程仍待用户主动验收。本次只做本地提交，未推送或核对远端 CI。

## 本轮已处理

探索增量：已实现三类搜索、平台推荐词、账号隔离历史、用户创作节目及单次订阅流程。真实只读搜索、翻页、推荐词、用户作品及订阅状态已核验；真实订阅写入和读回需由用户在独立候选程序中主动完成，Windows 原生界面尚未验收。详见 [探索说明](DISCOVERY.md) 和 [测试报告](TEST_REPORT.md)。

| 项目 | 结果 |
|---|---|
| 收藏加载失败 | 补充每次客户端运行的随机 UUID 头；真实请求由 400 变为 200 |
| 订阅/收藏分页结束 | 适配实测末页省略游标；本次读取收藏 10 条、订阅 40 条，无重复 |
| 缓存与诊断 | 内存完整缓存按 10 分钟过期；区分缓存/实时结果；嵌套列表错误纳入脱敏诊断 |
| 构建校验 | 临时双模块 workspace 修复 missing ziphash；根模块与宿主分别检查 |
| 不打扰桌面的开发验证 | -Candidate 独立输出、跳过绑定生成；浏览器测试静音、headless、独立配置 |
| 漏洞与依赖材料 | Go 1.26.8、修复后的传递依赖；实际候选扫描无命中；20 项 SBOM，63 份含嵌套声明及 Go 运行时的许可证文本，逐份核对哈希 |
| 短信认证取消 | 支持按钮/关闭/Escape；旧请求失效、完成竞态提示、重开隔离和旧状态刷新保护均通过合成回归 |
| 隔离存储故障 | Windows 系统 SQLite：中文路径重开、页数上限触发 SQLITE_FULL、双连接写锁冲突、子进程突然退出恢复均通过；损坏数据库逐字节保持原样 |
| 远端更新 | 已持续推送 main；截至 b33e8f7 的 CI 全绿，覆盖核心/浏览器/安装、Windows 构建、扫描和依赖材料归档 |
| 安装/卸载脚本 | 绝对路径和重解析点保护、暂存后原子升级、候选进程保护；PS 5.1 / 7 隔离测试 7 项通过 |
| 弹窗键盘操作 | 补齐可访问标题和关闭按钮名称；Enter 打开、Tab 双向循环、Escape 与焦点返回通过合成浏览器验证 |
| 公开节目列表 | 修复实际页面的 podcast.episodes 嵌套结构；两个匿名公开样本的详情和 1/15 条预览通过，保持分页未完成状态 |
| 渐进缓冲续播 | 目标范围不足或赋值暂时失败时保留续播位置，早期播放不会覆盖旧检查点，成功手动定位取代自动续播 |
| 播放和进度错误 | 写盘恢复清除对应错误且不掩盖媒体问题；旧选择/账号的保存结果和旧恢复播放拒绝不能污染当前状态；30 项前端测试通过 |
| 真实媒体范围响应 | 两个匿名媒体源的四次 206 Range 均匹配，共读取 64 字节；未播放音频，不代替原生编解码验收 |
| 合成常见格式 | Chrome 中 MP3/M4A/AAC 的 Range/非 Range 共 6 组通过；不能定位时解释并禁用相关操作，显示实际时间、保留旧进度直至自然播放追平 |
| 媒体失效和取消 | 四组真实浏览器 403/媒体错误验证有限重解析、进度保留、暂停/切歌隔离；迟到及显式暂停不再覆盖播放失败状态 |
| 并发播放请求 | 后端代次水位拒绝旧解析，取消排队请求、乱序到达和账号隔离通过 Go 回归；页面重载续播与定位失败反馈通过主界面检查 |
| 文档一致性 | 同步真实接口、构建、DPAPI 与浏览器证据，保留未验收边界 |

## 后续按此顺序推进

| 顺序 | 遗留项与具体完成标准 | 当前限制 / 验证方式 |
|---|---|---|
| 1 | 在用户方便时启动候选程序，看到收藏、加载全部、选集播放、退出重启后恢复暂停位置；与手机同期核对两页以上库 | 本轮只验证后台真实读取与合成浏览器；没有启动候选、操作用户桌面或核对手机 |
| 2 | 完成真实扫码闭环、过期续期与轮换、403/429/断网提示、两账号/访客隔离 | 合成测试已有；需要获准账号和自然过期/受控故障环境，不强制失效用户当前凭据 |
| 3 | 在原生 WebView2 验收认证取消；核实短信认证的人机验证流程 | 独立取消与关闭/重开、迟到响应已实现并通过合成回归；真实短信未验证，不盲发验证码 |
| 4 | MP3/AAC/M4A、Range/非 Range、超长音频、定位倍速；按 PRD 12.1 完成 3 轮各 4 小时持续收听，无崩溃/自发停播，网络中断可提示和恢复；隐藏窗口与锁屏不中断 | 两个匿名真实源的 206 探测与 Chrome 中三种格式的 6 组短合成媒体通过；非 Range 降级和续播保护已修复。真实 WebView2 编解码/长时播放仍待实测；2 小时只能作阶段检查，不满足最终门槛 |
| 5 | 托盘菜单、Explorer 重启恢复、实体媒体键冲突、睡眠唤醒、蓝牙/设备切换 | 需独占或约定实机测试时间；不在用户游戏期间执行系统消息、媒体键或睡眠测试 |
| 6 | 中文路径、磁盘满/损坏/占用恢复、跨 Windows 用户 DPAPI 隔离、正常退出/强退进度 | 中文路径、容量上限/锁冲突后的数据保留和恢复、损坏文件保留、突然退出的事务恢复已在临时目录验证；真实文件系统耗尽、跨用户 DPAPI、桌面最终进度仍待隔离实机验收 |
| 7 | 100/125/150/200% DPI、键盘焦点、屏幕阅读器、最小窗口；无 WebView2/离线/无管理员权限 | 弹窗命名、键盘焦点循环和关闭后返回已通过合成回归；实际屏幕阅读器、DPI 和干净 VM 仍需实测，不改用户当前显示缩放 |
| 8 | 当前用户安装、升级占用、保留/删除数据卸载、签名与正式发布 | 临时目录已验证安装/升级、文件占用保留旧版、链接拒绝、保留/删除数据卸载；真实运行中程序/干净 VM、签名凭据与正式发布仍待验证，未改动实际安装 |
| 9 | G1 平台登录/个人库/播放/分发许可、商标内容许可和专业评估；依赖嵌套许可证/编解码运行时条款审查 | 嵌套声明和 Go 运行时材料已收集；仍由项目负责人取得适用授权/专业结论并核实系统/编解码条款，SBOM与漏洞扫描不代替许可 |
| 10 | 核实远端 CI 成功、保存对应提交和构建哈希 | 截至 b33e8f7 的 CI 全绿，已包含 Linux 浏览器回归及独立 Windows 安装测试；r7 构建及 SHA-256 已记录，每批后续修改继续核对各自提交的 Actions，不以旧提交结果代替 |
| 11 | PRD 12.1 量化验收：5 名受邀者完成核心流程；30 次冷启动 p95 ≤ 3 秒；50 次合法音频首次出声 p95 ≤ 3 秒；500 条列表时应用及 WebView 子进程稳态工作集目标 ≤ 300 MB；正常续播误差 ≤ 5 秒；发布物/数据库/诊断隐私安全 | 尚无符合样本数和基线的实机测量，也未组织受邀测试者；需记录 Windows 11 x64/8 GB/SSD、CPU、WebView2、带宽和上游延迟，失败样本不得隐去。按原 PRD 执行，不以合成测试数量或候选构建通过替代 |

G1、G2、G3 均仍未整体通过。每项证据记录环境、步骤、实际结果、日期、提交或候选 SHA-256；失败或缺少条件的项目保持未通过。
