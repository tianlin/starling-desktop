# 遗留工作与验收状态

更新：2026-09-12。依据 PRD、PRD_TRACEABILITY、REAL_ENVIRONMENT_CHECKLIST 和 TEST_REPORT 逐类整理。未改写 PRD 的 G1～G3 通过条件。

## 本轮已处理

| 项目 | 结果 |
|---|---|
| 收藏加载失败 | 补充每次客户端运行的随机 UUID 头；真实请求由 400 变为 200 |
| 订阅/收藏分页结束 | 适配实测末页省略游标；本次读取收藏 10 条、订阅 40 条，无重复 |
| 缓存与诊断 | 内存完整缓存按 10 分钟过期；区分缓存/实时结果；嵌套列表错误纳入脱敏诊断 |
| 构建校验 | 临时双模块 workspace 修复 missing ziphash；根模块与宿主分别检查 |
| 不打扰桌面的开发验证 | -Candidate 独立输出、跳过绑定生成；浏览器测试静音、headless、独立配置 |
| 漏洞与依赖材料 | Go 1.26.8、修复后的传递依赖；实际候选扫描无命中；19 项 SBOM/许可证文本集合 |
| 短信认证取消 | 支持按钮/关闭/Escape；旧请求失效、完成竞态提示、重开隔离和旧状态刷新保护均通过合成回归 |
| 隔离存储故障 | Windows 系统 SQLite：中文路径重开、页数上限触发 SQLITE_FULL、双连接写锁冲突、子进程突然退出恢复均通过；损坏数据库逐字节保持原样 |
| 远端更新 | 66bf853 已推送 main，GitHub Actions 34677155739 的 Linux 核心检查、Windows 构建/扫描/材料归档均成功；新增浏览器 CI 另行核验 |
| 安装/卸载脚本 | 绝对路径和重解析点保护、暂存后原子升级、候选进程保护；PS 5.1 / 7 隔离测试 7 项通过 |
| 文档一致性 | 同步真实接口、构建、DPAPI 与浏览器证据，保留未验收边界 |

## 后续按此顺序推进

| 顺序 | 遗留项与具体完成标准 | 当前限制 / 验证方式 |
|---|---|---|
| 1 | 在用户方便时启动候选程序，看到收藏、加载全部、选集播放、退出重启后恢复暂停位置；与手机同期核对两页以上库 | 本轮只验证后台真实读取与合成浏览器；没有启动候选、操作用户桌面或核对手机 |
| 2 | 完成真实扫码闭环、过期续期与轮换、403/429/断网提示、两账号/访客隔离 | 合成测试已有；需要获准账号和自然过期/受控故障环境，不强制失效用户当前凭据 |
| 3 | 在原生 WebView2 验收认证取消；核实短信认证的人机验证流程 | 独立取消与关闭/重开、迟到响应已实现并通过合成回归；真实短信未验证，不盲发验证码 |
| 4 | MP3/AAC/M4A、Range/非 Range、超长音频、定位倍速、连续 2 小时播放；隐藏窗口与锁屏不中断 | 需要真实 WebView2 和允许测试的音频；当前合成 WAV 检查不替代此项 |
| 5 | 托盘菜单、Explorer 重启恢复、实体媒体键冲突、睡眠唤醒、蓝牙/设备切换 | 需独占或约定实机测试时间；不在用户游戏期间执行系统消息、媒体键或睡眠测试 |
| 6 | 中文路径、磁盘满/损坏/占用恢复、跨 Windows 用户 DPAPI 隔离、正常退出/强退进度 | 中文路径、容量上限/锁冲突后的数据保留和恢复、损坏文件保留、突然退出的事务恢复已在临时目录验证；真实文件系统耗尽、跨用户 DPAPI、桌面最终进度仍待隔离实机验收 |
| 7 | 100/125/150/200% DPI、键盘焦点、屏幕阅读器、最小窗口；无 WebView2/离线/无管理员权限 | 需要桌面或干净 VM；不能改用户当前显示缩放来代替隔离测试 |
| 8 | 当前用户安装、升级占用、保留/删除数据卸载、签名与正式发布 | 临时目录已验证安装/升级、文件占用保留旧版、链接拒绝、保留/删除数据卸载；真实运行中程序/干净 VM、签名凭据与正式发布仍待验证，未改动实际安装 |
| 9 | G1 平台登录/个人库/播放/分发许可、商标内容许可和专业评估；依赖嵌套许可证/编解码运行时条款审查 | 由项目负责人取得适用授权/专业结论；SBOM与漏洞扫描不代替许可 |
| 10 | 核实远端 CI 成功、保存对应提交和构建哈希 | 66bf853 / e0ab02d 的 CI 均全绿，后者含 Linux 浏览器回归；每批后续修改继续核对各自提交的 Actions，不以旧提交结果代替 |

G1、G2、G3 均仍未整体通过。每项证据记录环境、步骤、实际结果、日期、提交或候选 SHA-256；失败或缺少条件的项目保持未通过。
